<Window x:Class="GoogleCluodApiTest.Views.TranslationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoogleCluodApiTest.Views"
        mc:Ignorable="d"
        Title="TranslationWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Sourece" VerticalAlignment="Center"/>
            <TextBox Width="80" Text="{Binding SourceLang, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="자동감지" Command="{Binding DetectCommand}"/>
            <TextBlock Text="Target" Margin="16,0,0,0" VerticalAlignment="Center"/>
            <ComboBox Width="220"
                      ItemsSource="{Binding Languages}"
                      SelectedValuePath="Code"
                      DisplayMemberPath="Display"
                      SelectedValue="{Binding TargetLang}"/>
            <TextBlock Text="{Binding IsBusy, StringFormat='처리중: {0}'}" Margin="16,0,0,0" />
        </StackPanel>

        <TextBox Grid.Row="1"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Grid.Row="2" Height="40" Content="번역"
                Command="{Binding TranslateCommand}"/>

        <TextBox Grid.Row="3"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True" Text="{Binding OutputText}"/>
    </Grid>
</Window>
